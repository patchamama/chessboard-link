packages:
  - "packages/*"
  - "apps/*"

# esbuild ships a postinstall that fetches its native binary; allow it so
# vitest/tsup/vite can run. (The parent monorepo allows the same.)
onlyBuiltDependencies:
  - esbuild
