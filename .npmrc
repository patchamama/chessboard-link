# Don't abort scripts over the esbuild build-script warning inherited from the
# parent monorepo. esbuild's native binary is allowed via pnpm-workspace.yaml
# (onlyBuiltDependencies); this just stops the pre-run dependency verification
# from treating that warning as a fatal error.
verify-deps-before-run=false
